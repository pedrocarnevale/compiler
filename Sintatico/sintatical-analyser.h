#pragma once
#include <vector>
#include <unordered_map>

// functions
extern void parseFunction(std::vector<std::unordered_map<int,int>>& actionTable, std::vector<int>& ruleSize, std::vector<int>& ruleLeftPart);